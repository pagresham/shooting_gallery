
jQuery(document).ready(function($) {
	// Call the owl carousel function and settings //
	$('.owl-carousel').owlCarousel({
		navigation : true,
		loop:true,
	    singleItem: true,
	    item: 1,
	    autoPlay : 5000
	});
});